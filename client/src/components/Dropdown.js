import React from 'react';
import { Link } from 'react-router-dom';

{/*Not sure if it should just be the dropdown box itself as the component or two separate dropdown boxes with the content in them*/}

const Dropdown = () => {
  return (
    <div className="dropdown-content">

    </div>
  );

};

export default Dropdown;
